@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model FeedApp.Core.Entities.General.Reply

@{
    ViewData["Title"] = "AddReply";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="d-grid d-md-flex justify-content-md-end">
    
    <a class="btn btn-secondary me-md-1" asp-action="ProblemIndex" asp-controller="Problem">Back</a>
</div>


@*@using (Html.BeginForm("AddReply", "Problem", FormMethod.Post))
{
    <input hidden name="Id" value="@Model.Id" />
    <input hidden name="UserId" value="@Model.UserId" />
    <h3>Give a Reply</h3>

    <div class="form-group">
        <label for="ReplyDesc">Reply: </label>
        <textarea class="form-control" id="ReplyDesc" name="ReplyDesc" rows="5"></textarea>
    </div>
    <div class="form-group">
        <label for="Status">Status: </label>
        <select class="form-control" id="Status" name="Status">
            <option value="2">Processing</option>
            <option value="5">Rejected</option>
            <option value="6">Unsolvable</option>
        </select>
    </div>
    <br />
    <div class="d-grid d-md-flex justify-content-md-end">
        <button type="submit" class="btn btn-success">Post</button>
        <a class="btn btn-secondary me-md-1" asp-action="ProblemIndex" asp-controller="Problem">Back</a>
    </div>

    @if (Model.Replies.Any())
    {
        <table class="table">
            <thead>
                <tr>
                    
                    <th>User Name</th>
                    <th>Previous Replies</th>
                    <th>Entry Date and Time</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var reply in Model.Replies.OrderByDescending(r => r.EntryDate))
                {
                    <tr>
                       
                        <td>@reply.Users.UserName</td>
                        <td>@reply.ReplyDesc</td>
                        <td>@reply.EntryDate </td>

                    </tr>
                   
                }
            </tbody>
        </table>
    }
    else
    {
        <br />
        <h4>No replies found!!</h4>
    }
}*@

<div class="row">
    
    <div class="col-md-6">
        @using (Html.BeginForm("AddReply", "Problem", FormMethod.Post))
        {
            <input hidden name="Id" value="@Model.Id" />
            <input hidden name="UserId" value="@Model.UserId" />

            <h3>Give a Reply</h3>

            <div class="form-group">
                <label for="ReplyDesc">Reply: </label>
                <textarea class="form-control" id="ReplyDesc" name="ReplyDesc" rows="5"></textarea>
            </div>
            <div class="form-group">
                <label for="Status">Status: </label>
                <select class="form-control" id="Status" name="Status">
                    <option value="2">Processing</option>
                    <option value="3">Solved</option>
                    <option value="5">Rejected</option>
                    <option value="6">Unsolvable</option>
                </select>
            </div>
            <br />
            <div class="d-grid d-md-flex justify-content-md-end">
                <button type="submit" class="btn btn-success">Post</button>
            </div>
        }
    </div>

    
    <div class="col-md-6">
        @if (Model.Replies.Any())
        {
            <h3>History</h3>
            <br />
            <table class="table">
                <thead>
                    <tr>

                        <th>User Name</th>
                        <th>Replies</th>
                        <th>Entry Date and Time</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var reply in Model.Replies)
                    {
                        <tr>

                            <td>@reply.Users.UserName</td>
                            <td>@reply.ReplyDesc</td>
                            <td>@reply.EntryDate </td>
                        </tr>

                    }
                </tbody>
            </table>
        }
        else
        {
            <h4 style="color:crimson">No replies found!!</h4>
            <img src="~/facepalm-disappointed.gif" alt="Image Description" class="small-image" />
        }
    </div>
</div>



