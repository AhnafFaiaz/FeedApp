@*@model FeedApp.Core.Entities.General.Users


<h1>Signin</h1>
@using (Html.BeginForm("Signin", "Signin", FormMethod.Post))
{
    
    <div class="form-group">
        @Html.LabelFor(m => m.Email)
        @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Email)
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.Password)
        @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Password)
    </div>
    <br />

    <button type="submit" class="btn btn-primary">Sign-in</button>
}
<p>Don't have an account? <a href="@Url.Action("Signup", "Signup")">Sign-up</a></p>

*@

@*@model FeedApp.Core.Entities.General.Users

<h1 class="Signin">Sign In</h1>

@using (Html.BeginForm("Signin", "Signin", FormMethod.Post))
{
    <div class="form-group">
        @Html.LabelFor(m => m.Email, new { @class = "control-label" })
        @Html.TextBoxFor(m => m.Email, new { @class = "form-control", placeholder = "Enter your email" })
        @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.Password, new { @class = "control-label" })
        @Html.PasswordFor(m => m.Password, new { @class = "form-control", placeholder = "Enter your password" })
        @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
    </div>

    <button type="submit" class="btn btn-primary">Sign In</button>
}

<p class="signup-link">Don't have an account? <a href="@Url.Action("Signup", "Signup")">Sign Up</a></p>
*@
@model FeedApp.Core.Entities.General.Users
<!DOCTYPE html>
<html>
<head>
    <title>Signin</title>
    <link rel="stylesheet" type="text/css" href="~/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Jost:wght@500&display=swap" rel="stylesheet">
</head>
<body>
    
        <div class="main">
            <input type="checkbox" id="chk" aria-hidden="true">
        
        <label for="chk" aria-hidden="true">Sign-in</label>
        @using (Html.BeginForm("Signin", "Signin", FormMethod.Post))
        {
            <input type="Email" name="Email" placeholder="Email" required="">
            <input type="Password" name="Password" placeholder="Password" required="">

            <button type="submit" class="btn btn-primary">Sign In</button>
        }
</body>
<p style="color:bisque">  Don't have an account? <a style="color:aliceblue" href="@Url.Action("Signup", "Signup")">Sign Up</a></p>
@*<script>
    $(function () {
        var backlen = history.length;
        history.go(-backlen);
    });
</script>*@
</html>

